on after_initialization do
  add_rule("Rules", "Tobi", "After a round, if anyone has less than %{below} points, the game ends.", %{below: !below})
end

define_const tobi_below, !below

on before_start do
  as everyone do
    if current_score < @tobi_below do
      end_game
    end
  end
end