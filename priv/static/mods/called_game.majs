on after_initialization do
  add_rule("Rules", "Called Game", "After a round, if anyone has more than %{above} points, the game ends.", %{above: !above})
end

apply set, "score_calculation.called_game", !above
