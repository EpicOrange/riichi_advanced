on before_win do
  make_responsible_for("others")
end

define_scoring win do
  cond do
    won_by_discard and seat_is("last_discarder") -> total *= 2 :: "Discarder pays double"
    won_by_call and seat_is("last_caller") -> total *= 2 :: "Caller pays double"
  end
end