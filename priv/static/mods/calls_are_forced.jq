.after_initialization.actions += [
  ["add_rule", "Rules", "Calls Are Forced", "All calls are forced. If you have multiple call choices, you can still choose between them."]
]
|
.buttons[] += {"unskippable": true}
