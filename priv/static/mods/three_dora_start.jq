# add to initial dora flip
if .revealed_tiles[0] == -6 then
  .revealed_tiles += [-8, -10]
else . end
|
# same, but for sanma
if .revealed_tiles[0] == -10 then
  .revealed_tiles += [-12, -14]
else . end
