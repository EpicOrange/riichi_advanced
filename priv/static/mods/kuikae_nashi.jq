.after_initialization.actions += [["add_rule", "Kuikae Nashi", "Swap-calling is disallowed. That is, if you call two tiles, you cannot discard a tile that could have made a set with those two tiles. (e.g. calling a red five and discarding a normal five)"]]
|
.play_restrictions += [[["any"], ["just_called", {"name": "last_called_tile_matches", "opts": ["kuikae"]}]]]
