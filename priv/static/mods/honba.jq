.after_initialization.actions += [["add_rule", "Honba", "The 100 point sticks in the middle count honba. Each adds \(3 * $value) extra points to the winning value. One honba stick is added for every drawn round, and reset to 0 on a win."]]
|
.display_honba = true
|
.score_calculation.honba_value = $value
